> new task
> next stuff
one
three
> new stuff
> accept commands from input/external file (e.g. custom 'calculate score' scripts)
V work locally when local
V work globally otherwise or with home if no setup
> home can be used to setup global env without env variables:q
X flags can  x x x x x x x x x x x x x x x x x x x x x x x x x x
V avoid upper case switches
> mark score
> calculate score for list
> accept commands from input/external file (e.g. custom 'calculate score' scripts)
V work locally when local
V work globally otherwise or with home if no setup
> home can be used to setup global env without env variables:q
X flags can  x x x x x x x x x x x x x x x x x x x x x x x x x x
